version: '3.4'

services:
  mycatalogdb:
    image: mongo

  mycatalog.api:
    image: ${DOCKER_REGISTRY-}mycatalogapi
    build:
      context: .
      dockerfile: Services/Catalog/Catalog.API/Dockerfile

volumes:
  mongo_data: